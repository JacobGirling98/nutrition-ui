import React from "react"
import { render, screen, userEvent } from "@testing-library/react"
import Info from "./Info"

describe("Info component", () => {})

// describe('Info component', () => {
//   it('should render InfoContent component when button is clicked', async () => {
//     const mockDate = '2024-04-28';
//     render(<Info date={mockDate} />);

//     // Find the button
//     const button = screen.getByRole('button');

//     // Simulate button click
//     userEvent.click(button);

//     // Ensure InfoContent is rendered
//     await expect(screen.getByText(`App deployed at: ${mockDate}`)).toBeInTheDocument();
//   });
// });
